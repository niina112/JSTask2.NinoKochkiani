let age = -21;
let ticketPrice;

if (age <= 0 || age > 120) {
    console.log(`Wrong Age`);
} else if (age <= 12) {
    console.log(`price - ${ticketPrice = 5}$`);
} else if (age <= 17) {
    console.log(`price - ${ticketPrice = 10}$`);
} else if (age <= 64) {
    console.log(`price - ${ticketPrice = 15}$`);
} else {
    console.log(`price - ${ticketPrice = 8}$`);
}